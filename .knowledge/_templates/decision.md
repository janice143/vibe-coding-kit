# Decision: `[short-name]`

> **One-sentence summary.** The choice that was made, with the reason and the date so the next agent does not relitigate it.

**Decided:** YYYY-MM-DD
**Decided by:** `<name or role>`
**Status:** active · superseded by `<link>` · reverted

## Context

What was true at decision time. The options that were considered.

## Decision

What was chosen. State it in the present tense, as a rule, not as a story.

## Why

The reason. Concrete enough that the next person can decide whether the reason still holds.

## Consequences

What this decision costs and what it buys. Trade-offs the team accepted.

## When to revisit

The signal that should trigger a re-evaluation. A Taro upgrade, a platform change, a metric, a scale threshold — anything specific.

## See also

- `<link to related decisions>`
- `<link to the design-system / contract entry that codifies this>`
- `<link to the commit where this was first implemented>`
