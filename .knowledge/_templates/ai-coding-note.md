# AI coding note: `[short-name]`

> **One-sentence summary.** A factual observation about how coding agents behave on this project, with the rule or skill that operationalizes it.

**Observed:** YYYY-MM-DD (or range)
**Agents involved:** `<Claude Code, OpenCode, Codex, …>`
**Task type:** `<small change, refactor, prototype conversion, design-system edit, …>`

## What the agent did

The behavior. Concrete and observable. No characterization of intent.

## Why it is the default

The mechanism: what in the agent's training or loop produces this default. Useful for predicting when the same default will recur.

## What it cost

Concrete cost: time, lines of code, files touched, regressions introduced.

## What to do instead

The rule, skill, or knowledge entry that intercepts the default. If none exists, propose one.

## See also

- `<link to AGENTS.md rule>`
- `<link to SKILL.md>`
- `<link to related ai-coding-note>`
