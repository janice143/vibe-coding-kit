# Pattern: `[short-name]`

> **One-sentence summary.** The validated, project-specific shape that should be the default next time.

**Status:** active · since YYYY-MM-DD
**Last validated:** YYYY-MM-DD
**Used in:** `<list of file paths / pages / commits where this pattern is currently applied>`

## What it is

A short description of the pattern: what shape it has, when it applies, and what problem it solves.

## Why it works

The reason this is the right shape — usually a platform constraint, a framework guarantee, or a previous-bug postmortem.

## When to use

Concrete triggers. If a task matches one of these, reach for this pattern.

## When **not** to use

Equally important. Where the pattern is the wrong tool, or where another pattern supersedes it.

## Example

A small code or config snippet showing the canonical shape. Keep it minimal — the value is in the shape, not the verbosity.

## Anti-shape (what to avoid)

The shape that looks similar but causes the bug or the drift. Concrete, not abstract.

## See also

- `<link to AGENTS.md section>` — if a rule codifies this pattern.
- `<link to SKILL.md>` — if a skill invokes this pattern.
- `<link to platform note>` — if a platform fact motivates this pattern.
