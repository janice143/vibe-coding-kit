# Anti-pattern: `[short-name]`

> **One-sentence summary.** The shape that should not recur, and the cost it imposes.

**Status:** active · since YYYY-MM-DD
**Last observed:** YYYY-MM-DD (most recent incident where this anti-pattern caused a bug or cost time)
**Cost observed:** `<hours lost, files touched, or other concrete measure>`

## What it looks like

The shape in code or config. Concrete enough that the next agent recognizes it on sight.

## Why it is wrong

The actual mechanism: which assumption breaks, which contract is violated, which platform fact is contradicted.

## What to do instead

Point to the right pattern (link to `patterns/<name>.md`) or the right skill. If no replacement exists, name the manual fix.

## How it slipped in

The reason this shape got written in the first place. Usually "looks fine in the simulator", "matches the Web intuition", "AI default completion", or "first version, refactored later".

## See also

- `<link to pattern it should be replaced by>`
- `<link to incident / commit where this cost time>`
- `<link to AGENTS.md or skill that codifies the rule>`
